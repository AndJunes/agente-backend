# The curated bundle for `docker/runner.Dockerfile`. Fixed and pinned on purpose: this image
# never runs `pip install` at generation time, only at build time, here. Versions are what was
# already validated end-to-end on the host this session (a real FastAPI+pytest project running
# through the real certifier, not just importable).
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
requests==2.34.2
httpx==0.28.1
Flask==3.1.3
SQLAlchemy==2.0.54
python-multipart==0.0.32
email-validator==2.3.0
pytest==9.1.1
