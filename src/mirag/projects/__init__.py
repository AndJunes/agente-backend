"""Whole generated projects: model, generation, certification, packaging and artifacts.

    GENERATED -> VALIDATED -> EXECUTED -> TESTED -> VERIFIED
                                              \\-> PARTIAL
                                              \\-> FAILED

The project object is the single source: the workspace where it runs, the manifest that is
shown and the ZIP that is downloaded all come from it. If each were built on its own, the
defect that took two phases to close would come back: measuring one thing and serving another.
"""
